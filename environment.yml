name: stack-digital-core
channels:
  - defaults
dependencies:
  - python=3.8
  - pip
  - setuptools
  - wheel
  - pillow
  - scikit-image
  - pip:
    - pycaret[full]
    - pytest
    - nbmake
prefix: /Users/nick/miniconda3/envs/stack-digital-core
