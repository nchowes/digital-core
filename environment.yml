name: stack-digital-core
channels:
  - defaults
dependencies:
  - python=3.8
  - pip
  - pillow
  - scikit-image
  - pip:
    - wheel
    - setuptools
    - pycaret[full]
    - pytest
    - nbmake
prefix: /Users/nick/miniconda3/envs/stack-digital-core
